# 2026-09-20 四小时推进：AgentFEM 力学契约闭环

## 单一目标

让 AgentFEM 通过版本化、可预检、可解释的 Kernel Contract 调用现有 T3/T4
线性力学主链，而不是新增一套计算实现。

## 工作序列

1. 冻结 0.2 请求、结果、错误和兼容边界，记录 ADR。
2. 实现二维/三维网格、材料、载荷、约束和过程解析。
3. 接通 `ExecutionPlan -> execute_plan -> ExecutionReceipt`。
4. 返回位移、应变、应力、守恒量、运行身份、成熟度和摘要证据。
5. 增加版本化 Schema、CLI 预检和力学 VTK。
6. 用 0.1 回归、T3、T4、失败路径和包内 Schema 做高信息测试。
7. 运行定向测试、全量测试、格式检查和独立性检查；只在里程碑边界推送一次。

## 验收边界

- 0.1 请求行为保持兼容；
- T3/T4 均能先规划后执行，规划和执行摘要一致；
- 结果完全 JSON 安全，路径可定位，产物不能逃逸根目录；
- 成功执行不提升科学成熟度；
- 不增加运行依赖，不读取第三方有限元实现源码；
- 重要新增注释、文档字符串、诊断和说明以中文为主。
