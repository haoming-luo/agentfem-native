# 五小时冲刺：重复约束求解生命周期 0.1

**目标：** 把已经完成的预备 T3/T4 装配继续推进到固定矩阵多载荷的完整稀疏求解。

## 工作包

1. 固化数学规格和生命周期 ADR，明确固定图、固定约束与固定数值的边界；
2. 实现可复用约束图分析、独立右端项变换和只读结构摘要；
3. 实现绑定矩阵数值的 Jacobi/块 Jacobi 预条件器与原 CG 兼容入口；
4. 为 `NativeSparseProvider` 增加显式准备和重复求解计划，不改变通用提供者协议；
5. 用少量高信息单元测试覆盖数学等价、复用、缺失对角和错误失配；
6. 建立 T3/T4 完整多载荷基准，分离一次准备成本与稳态耗时；
7. 运行格式、独立性、全量本地测试，保存证据并更新路线图和高管汇报；
8. 按 CI 节流规则推送一个源代码候选，由 Linux 快检验收，不重复启动全平台矩阵。

## 完成判据

- 冷/预备约束矩阵和右端项逐项一致，解达到原有容差；
- 固定矩阵重复求解期间复用约束后 CSR 图和同一数值预条件器；
- T3/T4 基准保存可复现实验参数、正确性字段、准备成本和稳态加速比；
- 本地全量测试、Ruff 和独立性检查全部通过；
- 代码、证据和后续计划均保存在仓库并推送。
