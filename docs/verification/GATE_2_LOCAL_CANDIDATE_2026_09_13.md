# Gate 2 本地科学验收候选报告

**日期：** 2026-09-13  
**平台：** macOS arm64  
**版本：** AgentFEM Native `0.7.0a1`、C++ ABI 1.2  
**源提交：** `d9c4ced`
**结论：** T3/T4 本地科学验收候选通过；Gate 2 整体成熟度仍为 `implemented`

## 本轮新增或强化的证据

- 确认原 T3/T4 制造解案例已经执行装配、约束、稀疏求解与恢复，不重复增加同类
  测试；测试名称改为准确描述“实际求解收敛”；
- T3 三层网格制造解同时检查二阶误差趋势、求解器收敛、尺度化自由残差和全局
  载荷—反力平衡；
- 原 T3 中心节点 patch 改为非规则内部节点，保持仿射位移、应变、应力和自由
  残差精确性；
- T4 三层网格制造解增加尺度化自由残差与全局平衡检查；
- 新增 T4 合法畸变网格 patch：移动立方体内部节点，同时保持边界不变，验证
  仿射位移、六分量工程应变、自由残差和全局平衡；
- 执行收据新增 `analysis` 摘要，明确记录问题类型、空间维数、单元族、DOF 数、
  装配路径、提供者和真实成熟度；预装配动力系统的未知维数明确返回 `null`。

## 验证结果

- 聚焦 T3、T4、计划与执行证据：41 项通过；
- 完整 Python 套件：170 项通过，用时约 0.45 秒；
- 独立性扫描：通过，无禁止的第三方有限元实现导入；
- Ruff lint：通过；格式检查在自动格式化后通过；
- C++20 数值契约：Clang 警告视为错误，编译与执行通过；
- C11 公共头文件契约：编译、链接与执行通过；
- AddressSanitizer 与 UndefinedBehaviorSanitizer：编译与执行通过。
- `cp311-abi3` macOS arm64 wheel 构建并安装到隔离 Python 3.12 环境；安装后的
  C++ ABI 1.2 可用，T3 计划—执行收据包含预期的七项 `analysis` 字段。
- 普通远端 Linux 快检
  [GitHub Actions 运行 34750325307](https://github.com/haoming-luo/agentfem-native/actions/runs/34750325307)
  在源提交 `d9c4ced` 上通过安装、格式、完整测试、便携 CLI 契约和独立性检查；
  其余 Tier-1 任务按节流策略跳过。

## 三档本地性能记录

每档均为五次热进程运行的中位数。参考实现使用本项目维护的有限元数学与 NumPy
数组运算；NumPy 本身不是项目自研对象。完整原始记录保存在相应 JSON 文件中。

| T3 规模 | DOF / 单元 | 参考装配 | C++ 装配 | 加速 | 最大相对矩阵差异 |
|---|---:|---:|---:|---:|---:|
| 小 | 2,178 / 2,048 | 0.107080 s | 0.000216 s | 496.1× | 0 |
| 中 | 8,450 / 8,192 | 0.440521 s | 0.000891 s | 494.4× | 7.65e-16 |
| 大 | 33,282 / 32,768 | 1.664815 s | 0.003437 s | 484.4× | 4.59e-16 |

三档 C++ CSR SpMV 相对可读归约约为 2.06–2.13×。中档固定 T4 案例的 C++ 体积
装配为 0.000556 秒，参考实现为 0.007824 秒，加速约 14.1×；最大矩阵差异
`5.68e-14`，求解后的全局平衡范数为 `1.78e-14`。

原始记录：

- `benchmarks/results/2026-09-13-darwin-arm64-gate2-local-small.json`；
- `benchmarks/results/2026-09-13-darwin-arm64-gate2-local-candidate.json`；
- `benchmarks/results/2026-09-13-darwin-arm64-gate2-local-large.json`。

## 声明边界与剩余工作

- 性能数字仅属于本机热进程测量，不能外推到 Windows、Linux 或其他硬件；
- 本轮证明当前 T3/T4 数学与实现证据形成了可审查的本地闭环，但不是实验验证，
  也不是任意几何、材料和边界条件的通用声明；
- Kernel Contract 0.1 仍只接纳稳态扩散；力学直接 API 已有 Agent 证据，但
  版本化 JSON Contract 0.2 尚未接纳；
- P3 CPU 预构建稀疏图、数值回填和确定性线程化尚未完成；
- 因此 Gate 2 不提升成熟度。下一步先完成 P3 CPU Production Spine，再提交一次
  Gate 2 Windows/Linux/macOS Tier-1 候选验收。
